/** @type {import("prettier").Config} */
const prettierConfig = {
  trailingComma: "es5",
  tabWidth: 2,
  semi: true,
  singleQuote: true,
  printWidth: 100,
};

export { prettierConfig };

export default prettierConfig;
